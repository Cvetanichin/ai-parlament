---
status: SPECIFIED — DRAFT, pending Product Owner approval — see APIs-Specification-v1.0.md
eas_reference: EAS v1.0 §3.4 (API Gateway), §6.3 (Regulatory APIs)
---
# 12 — APIs

Full specification: `APIs-Specification-v1.0.md` — the Gateway's
cross-cutting contract (versioning, auth, error shape, pagination, rate
limiting) plus a consolidated endpoint catalog pointing to each owning
spec. Does not redefine any endpoint's business contract — every one is
already specified elsewhere; this is the routing index.
